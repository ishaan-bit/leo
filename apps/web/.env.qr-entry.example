# QR Entry & Identity System
# Add these to your existing .env.local

# Feature Flags
NEXT_PUBLIC_BIND_ENABLED=true
LEO_QR_ENTRY_ENABLED=true

# NextAuth (required for bind flow)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-use-openssl-rand-base64-32

# Email Provider (for magic link bind)
# Option 1: SMTP
EMAIL_SERVER_USER=your-smtp-user
EMAIL_SERVER_PASSWORD=your-smtp-password
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_FROM=noreply@yourdomain.com

# Option 2: SendGrid (alternative)
# SENDGRID_API_KEY=your-sendgrid-key

# Google OAuth (for bind flow)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Vercel KV (Redis)
# Already configured if using Vercel
KV_REST_API_URL=your-kv-url
KV_REST_API_TOKEN=your-kv-token
